dist: xenial
language: python
python:
  - "2.7"
install:
  - sudo apt update
#   - sudo apt install python3-dev python3-pip
  - pip install --upgrade pip
#   - pip3 install --upgrade --force-reinstall setuptools
  - pip install keras
  - pip install tensorflow
script:
#   - sudo find /usr -name '*.pyc' -delete
  - python model.py
  - ls -l *
  - printenv
deploy:
#   - curl -T <FILE.EXT> -uregmar:<API_KEY> https://api.bintray.com/content/regmar/tfpy/<YOUR_COOL_PACKAGE_NAME>/<VERSION_NAME>/<FILE_TARGET_PATH>

